class Solution:
    def isPalindrome(self, s: str) -> bool:
        s = s.lower()
        s = "".join(filter(str.isalnum , s))
        if s[::-1] == s:
            return True
        else:
            return False    
