class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        maxx = 0
        total = 0
        for num in nums:
            if num == 1:
                total += 1
                maxx = max(total,maxx)
            else:
                total = 0    
        return maxx        
