class Solution:
    def isPalindrome(self, x: int) -> bool:
        
        digits = list(str(x))
        reverse = digits[::-1]

        if reverse == digits:
            return True
        else:
            return False    
